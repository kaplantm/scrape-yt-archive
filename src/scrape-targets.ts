import axios from "axios";
import { Snapshot } from "./types";

export const scrapeTargets = async (targets: { [key: string]: Snapshot }) => {
  // const
  //   for (const snapshot of targets) {
  // const contents = await axios.get()
  // console.log(snapshot);
  //   }
};
